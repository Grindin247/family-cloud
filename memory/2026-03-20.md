## 2026-03-20

- Fixed home-portal `New Doc` / `Whiteboard` ghost creations. Root cause was side-effectful `GET /launch/*` routes being linked with `next/link`, which allowed background prefetching to create empty files in `Notes/Inbox`.
- Converted doc/whiteboard launch actions to explicit `POST` form submissions, made `GET /launch/new-doc` and `GET /launch/whiteboard` harmless, and added stale empty auto-generated inbox cleanup in the launch route.
- Rebuilt and restarted the live `home-portal` container, then verified `Notes/Inbox` was empty after cleanup.
- Implemented and deployed the FileAgent inbox upgrade: shared inbox processor, manual Home Dashboard `Process Inbox` trigger, decision-api `POST /v1/family/{family_id}/files/process-inbox`, richer memory `source_refs`, and updated Nextcloud MCP tools for lock-aware directory listing and ready-tag removal.
- Rebuilt and restarted the live `nextcloud-mcp`, `decision-api`, and `home-portal` services; verified the new MCP tools were callable, the new inbox API route was present in the running OpenAPI schema, and the home page served the `Process Inbox` tile.
- Fixed the first live `Process Inbox` failure path: when Nextcloud's ready-tag REPORT fails, the inbox processor now logs and skips ready-tag discovery instead of crashing, so Home Dashboard docs can still process. Also updated the home-portal tile to ask for confirmation, show a processing banner/button state immediately, and use relative home redirects so failures no longer bounce users to `https://localhost:3000/...`.
- Added and deployed a Nextcloud MCP fallback for tag discovery: if `get_files_by_tag` fails with the upstream WebDAV `REPORT` 500, the overlay now scans the requested scope and checks tags per file. Verified live that `nc_webdav_list_ready_files` returns structured JSON again and logs the fallback path instead of surfacing an MCP error.
