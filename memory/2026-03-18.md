- Patched OpenClaw `sessions_spawn` payload construction in the installed runtime bundles under `/home/luvwrk777/.npm-global/lib/node_modules/openclaw/dist/` so child-session tasks resolve speaker identity from `/home/luvwrk777/.openclaw/family-identity.json` before spawn.
- The rewrite now canonicalizes `actor`, adds `actor_person_id` and `family_slug`, and annotates `metadata` with `canonical_actor_email` and `resolution_source`.
- Restarted `openclaw-gateway.service` at 2026-03-18 00:16 EDT so the patched runtime would take effect.
- Verified live via Amelia -> `file-agent` and Caleb/main -> `event-agent` that a prompt starting with `Momma here` now reaches child agents as Rachel's canonical account (`rachel.c.griffin@gmail.com`) instead of the raw nickname or James.
