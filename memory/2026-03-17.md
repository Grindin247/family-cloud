Finished Vikunja direct-edit webhook compatibility for task events.

- Root causes were duplicate webhook registrations on the same project and overly loose payload extraction that could mistake a nested project object for the task object.
- `ensure_project_webhooks()` now replaces duplicate hooks for the same target URL and leaves one canonical hook per project.
- `extract_task_payload()` now scores nested candidates and prefers task-shaped payloads over generic `{id, title}` objects.
- Verified live: a direct Vikunja update on task `283` immediately created canonical event `53802aa0bf383bb422c165808cca951a08110340f1aef4043bdb95d25f3f3d5d` with `event_type=task.updated`, `subject_id=283`, `source=Vikunja`.
- Existing bad test event for subject `53` remains in the database from before the payload-selection fix.

Flattened Nextcloud Notes PARA layout live and validated the FileAgent path.

- `/Notes/FamilyCloud` was fully pruned after migrating its remaining files into canonical `/Notes/{Inbox,Projects,Areas,Resources,Archive,Unfiled}`.
- `scripts/nextcloud_para_agent.py` needed live-compat fixes for Nextcloud DAV quirks: system tags expose `oc:display-name`, WebDAV `oc-fileid` headers are not suitable for tag relations, and MCP ready-file listing can fail transiently, so the agent now falls back to direct WebDAV tag scans and raw file reads.
- Live FileAgent run processed 2 tagged inbox files successfully: a readable markdown note was renamed to `/Notes/Projects/2026-03-17_132438_kitchen-remodel-project.md`; an unreadable generic PDF was moved to `/Notes/Unfiled/2026-03-17_132604_unfiled.pdf`; both lost the `ready` tag.
- Home portal launch routes still return valid 307 redirects for `new-doc` and `whiteboard`, but server-side proof that those generated files land in `/Notes/Inbox` remains incomplete without following the returned Nextcloud URL in a browser session.
